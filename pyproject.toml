[project]
name = "implement-transformer"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "torch==2.5.1",
    "numpy",
    "pandas",
    "matplotlib",
    "jaxtyping",
    "kagglehub",
    "ipykernel",
    "jupyter",
    "pytest",
]
